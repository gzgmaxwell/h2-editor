import elementClass from './enum.element.class'
import elementType from './enum.element.type'
import fileType from './enum.fileType'
import attrGroupId from './enum.attrGroupId'

export {
    elementType,
    elementClass,
    fileType
}

export default {
    elementType,
    elementClass,
    fileType,
    attrGroupId
}
