// import { elementType } from '../../../config/enum'

export default function () {
    this.$el.addEventListener('click', (e) => {
        // Vue.store.commit('ELEMENT_SELECT', { eqxElements: [this] })
        // this.addChildBorderStyle()
        // // 选中之后 再次点击层级下降为0
        // if (this.elementJson.css.zIndex !== 0) {
        //     this.elementJson.property.zIndex = this.elementJson.css.zIndex
        // }
        // this.elementJson.css.zIndex = 0
    })
}
